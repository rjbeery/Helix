{
  "name": "helix",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm --filter @helix/utils build && pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean",
    "check-bom": "node ./scripts/check-bom.js",
    "remove-bom": "node ./scripts/remove-bom.js",
    "install-hooks": "sh ./scripts/install-githooks.sh"
  }
  ,"devDependencies":{
    "typescript":"^5.6.0",
    "vite":"^5.0.0",
    "ts-node":"^10.9.1"
  }
}
