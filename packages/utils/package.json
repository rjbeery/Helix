{
  "name": "@helix/utils",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "sideEffects": false,
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "powershell -c \"if(Test-Path dist){Remove-Item dist -Recurse -Force}\""
  }
}
